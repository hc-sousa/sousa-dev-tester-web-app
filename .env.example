DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/DATABASE_NAME
POSTGRES_PASSWORD=changeme
ADMIN_PASSWORD=
# Canonical public URL (used in robots.txt and sitemap.xml)
SITE_URL=https://sousadev.com
# HTTP port for node index.js (macOS AirPlay often uses 5000; try 3000 if bind fails)
PORT=5000
APP_PORT=5000
